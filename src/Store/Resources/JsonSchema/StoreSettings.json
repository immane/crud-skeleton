{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "Store/StoreSettings",
  "title": "Store Settings",
  "description": "Store-local configuration (Store.settings). Both flows default to false.",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "fulfillment": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "requireVerification": {
          "type": "boolean",
          "description": "When true, a fulfilled Store order must be verified before it can be completed.",
          "default": false
        }
      },
      "required": []
    }
  },
  "required": []
}
